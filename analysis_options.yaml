include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "backup_*/**"
    - "_backup_*/**"
    - "**/backup_*/**"
    - "**/_backup_*/**"
    - ".dart_tool/**"
    - "build/**"

linter:
  rules:
    avoid_print: false
